import { CompdirectiveDirective } from './compdirective.directive';

describe('CompdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CompdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
